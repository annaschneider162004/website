<<<<<<< HEAD
# Apache configuration for MusicOfEveryone website
Options -Indexes

# Prevent direct access to includes folder
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^includes/ - [F,L]
</IfModule>

# Compress text files
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json
</IfModule>

# Browser caching
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>

# Custom error pages
ErrorDocument 404 /index.php
=======
# Apache / cPanel .htaccess – MusicOfEveryone Music Club
# =========================================================
# Tối ưu tốc độ và bảo mật cơ bản cho cPanel shared hosting

Options -Indexes

# -------------------------------------------------------
# Gzip Compression (mod_deflate)
# -------------------------------------------------------
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css
    AddOutputFilterByType DEFLATE text/javascript application/javascript application/x-javascript
    AddOutputFilterByType DEFLATE application/json application/xml application/xhtml+xml
    AddOutputFilterByType DEFLATE image/svg+xml font/ttf font/otf font/woff font/woff2
</IfModule>

# -------------------------------------------------------
# Browser Cache-Control for Static Assets
# -------------------------------------------------------
<IfModule mod_expires.c>
    ExpiresActive On

    # Images
    ExpiresByType image/jpeg                    "access plus 1 year"
    ExpiresByType image/png                     "access plus 1 year"
    ExpiresByType image/gif                     "access plus 1 year"
    ExpiresByType image/webp                    "access plus 1 year"
    ExpiresByType image/svg+xml                 "access plus 1 year"
    ExpiresByType image/x-icon                  "access plus 1 year"

    # CSS & JavaScript
    ExpiresByType text/css                      "access plus 1 year"
    ExpiresByType text/javascript               "access plus 1 year"
    ExpiresByType application/javascript        "access plus 1 year"

    # Fonts
    ExpiresByType font/woff                     "access plus 1 year"
    ExpiresByType font/woff2                    "access plus 1 year"
    ExpiresByType application/font-woff         "access plus 1 year"
    ExpiresByType application/font-woff2        "access plus 1 year"

    # HTML & PHP (không cache lâu)
    ExpiresByType text/html                     "access plus 0 seconds"
    ExpiresByType application/xhtml+xml         "access plus 0 seconds"
</IfModule>

# Cache-Control headers
<IfModule mod_headers.c>
    # Cache static assets aggressively
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|svg|woff|woff2|ico)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    # No-cache for HTML/PHP
    <FilesMatch "\.(html|php)$">
        Header set Cache-Control "no-cache, must-revalidate"
    </FilesMatch>
    # Security headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# -------------------------------------------------------
# Chặn truy cập trực tiếp vào thư mục includes/ và data/
# -------------------------------------------------------
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^includes/   - [F,L]
    RewriteRule ^data/       - [F,L]
</IfModule>

# -------------------------------------------------------
# PHP includes bên ngoài webroot (bảo vệ includes/)
# -------------------------------------------------------
<FilesMatch "^(db|config)\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# -------------------------------------------------------
# Custom Error Pages
# -------------------------------------------------------
ErrorDocument 404 /index.php
# ErrorDocument 403 /index.php
# ErrorDocument 500 /index.php

# -------------------------------------------------------
# Redirect HTTP → HTTPS (bỏ comment khi đã có SSL)
# -------------------------------------------------------
# RewriteEngine On
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]

# -------------------------------------------------------
# Block access to sensitive files
# -------------------------------------------------------
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|sql|bak)$">
    Order allow,deny
    Deny from all
</FilesMatch>
>>>>>>> origin/main
